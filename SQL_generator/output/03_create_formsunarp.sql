-- UP
CREATE TABLE FORMSUNARP
(
  ID NUMBER(10) NOT NULL,
  NRO_PROPIEDADES NUMBER(2) NOT NULL,
  NRO_VEHICULOS NUMBER(2) NOT NULL,
  OBSERVACIONES VARCHAR(200) NOT NULL,
  PRIMARY KEY (ID)
);

CREATE SEQUENCE FORMSUNARP_SEQ START WITH 1;

CREATE OR REPLACE TRIGGER FORMSUNARP_PK 
BEFORE INSERT ON FORMSUNARP 
FOR EACH ROW

BEGIN
SELECT FORMSUNARP_SEQ.NEXTVAL
INTO   :new.id
FROM   dual;
END;
/

-- DOWN
DROP SEQUENCE FORMSUNARP_SEQ;
DROP TABLE FORMSUNARP;
